{
  "name": "backend",
  "description": "Generated by cds init",
  "version": "1.0.0",
  "engines": {
    "node": "^12.x"
  },
  "files": [
    "db",
    "srv"
  ],
  "dependencies": {
    "@sap/cds": "latest",
    "express": "latest",
    "hdb": "latest"
  },
  "scripts": {
    "start": "cds run",
    "build": "cds build/all --clean",
    "deploy": "cds deploy"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "hana",
        "model": "db"
      }
    }
  }
}
